# 板块速览动态内容刷新凭证

## 捕获身份

| 字段 | 值 |
|---|---|
| Capture ID | `20260729-local-sector-overview-dynamic-1603` |
| 浏览器地址 | `http://127.0.0.1:5173/wealth/` |
| 页面状态 | 已登录的 `main.page-shell` loaded 页面；未见登录表单、DEV 调试面板或模块 error marker。 |
| 页面可见时钟 | `2026-07-29 16:03:11`（页面本地时钟） |
| Figma 目标节点 | `04 Market Overview - Desktop Loaded / 103:2` 中的 `SectorOverview / 106:123` |
| 证据范围 | 仅冻结当前板块速览的可见文本、数值、涨跌色和热力格动态填充；不替代 `20260728-local-loaded-1600-2x` 的几何量测基线。 |

## 动态内容口径

- 8 张 Top5 卡片共 40 条排行已逐条以当前页面可见内容回填。
- 5 x 4 热力图共 20 格已逐格以当前页面可见内容回填。
- 上涨数值色为 `#ff4d5a`；下跌数值和文本色为 `#15c784`。
- 当前 20 个热力格均为下跌状态，填充为 `#15c784`、`42%` opacity；这只是当前数据状态，不是热力图的固定产品配色。

## 产物

| 文件 | 用途 | SHA-256 |
|---|---|---|
| `full-page.png` | 当前已登录页面的动态内容取证截图。该文件是整页截图，不作为板块速览的新的几何裁图或像素 overlay 源。 | `2fa65cc0f9d72268aa273996e467cc3edb2b5943575fc09d877ac527e7c5720d` |

## Figma 回读结果

- `106:123` 保持 `1564 x 501px`；内部 layout 保持 `1538 x 430.5px`；热力图 preview 保持 `319.210998 x 382px`。
- 未改动 root、卡片或热力格的坐标、尺寸、Auto Layout、gap、padding、Panel 边框、圆角和字体。
- Figma 内所有更新后的数值、名称、涨跌色和 20 个热力格填充均已回读确认。

## 验收边界

本次通过的是动态内容映射和几何未漂移核验，状态为 `GEOMETRY_PASS`。尚未以同尺寸板块裁图完成新的透明叠图，因此不得标记 `STYLE_PASS` 或 `PIXEL_VERIFIED`。
