# <模块名> 编码前门禁模板（coding-gate）

> 用途：在编码前锁定“样例、查询、状态、异常、性能、签字清单”。  
> 阶段：M2 开工前。  
> 产物性质：执行门禁清单（不通过不允许编码）。

---

## 1. 目的

1. 本门禁对应模块：
2. 本门禁对应需求文档：
3. 本门禁对应实施方案：

---

## 2. 总门禁清单（全通过才能开工）

1. [ ] 参数与默认值冻结
2. [ ] 响应结构冻结
3. [ ] 核心样例响应冻结
4. [ ] 查询草案冻结
5. [ ] 状态归并样例冻结
6. [ ] 异常覆盖矩阵冻结
7. [ ] 性能预算冻结
8. [ ] 签字完成

---

## 3. 请求与响应冻结

### 3.1 请求参数冻结

```ts
interface <Module>Request {
  // ...
}
```

参数校验规则：

1. ...
2. ...

### 3.2 响应结构冻结

```ts
interface <Module>Response {
  // ...
}
```

---

## 4. 核心样例响应（最小集合）

### 4.1 正常样例

```json
{ "...": "..." }
```

### 4.2 delayed 样例

```json
{ "...": "..." }
```

### 4.3 empty 样例

```json
{ "...": "..." }
```

### 4.4 error 样例

```json
{ "...": "..." }
```

---

## 5. 查询草案（可直接转实现）

1. 主查询 SQL 草案：
2. 补列查询 SQL 草案：
3. 回退查询 SQL 草案：
4. 索引与排序说明：

---

## 6. 状态归并样例

| 模块状态组合 | 页面状态 | 备注 |
|---|---|---|
| `...` | `...` | `...` |

---

## 7. 异常码覆盖矩阵

> 异常码必须来自  
> `wealth/docs/system/exception-code-registry.md`

| code | 覆盖用例 | 触发条件 | 预期行为 |
|---|---|---|---|
| `<CODE>` | `<CASE>` | `<WHEN>` | `<EXPECT>` |

---

## 8. 性能门禁

1. P95 预算：
2. 返回体大小预算：
3. 最大并发预算：
4. 超预算降级策略：

---

## 9. 测试门禁

1. 单元测试：
2. 集成测试：
3. 冒烟测试：
4. debug 模式验证：

---

## 10. 签字清单

### 10.1 后端负责人

1. [ ] 查询草案可实现
2. [ ] 异常覆盖完整
3. [ ] 状态归并无歧义

### 10.2 前端负责人

1. [ ] 响应结构可消费
2. [ ] 状态表达可落 UI
3. [ ] 降级策略可实现

### 10.3 架构/产品负责人

1. [ ] 范围未扩散
2. [ ] 语义与业务一致
3. [ ] 可进入编码阶段

---

## 11. 版本记录

| 版本 | 日期 | 变更摘要 | 负责人 |
|---|---|---|---|
| v1 | `<YYYY-MM-DD>` | 初版 | `<owner>` |
