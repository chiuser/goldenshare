# <模块名> 标杆需求模板（benchmark-requirement）

> 用途：定义“这个模块到底要解决什么问题、边界是什么、规则归谁负责”。  
> 阶段：需求冻结前。  
> 产物性质：业务事实源（不是实现细节文档）。

---

## 1. 目标与定位

1. 模块目标（要解决的核心问题）：
2. 用户价值（对用户可见收益）：
3. 业务定位（在页面/系统中的角色）：

---

## 2. 范围与边界

### 2.1 本期覆盖

1. ...
2. ...

### 2.2 本期不覆盖

1. ...
2. ...

### 2.3 与其他模块边界

1. 上游依赖：
2. 下游消费者：
3. 与相邻模块的职责分割：

---

## 3. 核心原则（硬约束）

1. 规则归属（后端/前端谁定义）：
2. 契约归属（单一事实源文档）：
3. 禁止事项（本模块绝不允许）：

---

## 4. 业务对象模型（非代码，先语义）

1. 对象 A（定义 + 字段语义）：
2. 对象 B（定义 + 字段语义）：
3. 对象 C（定义 + 字段语义）：

字段必须说明：

1. 含义
2. 单位
3. 是否可空
4. 谁负责产出
5. 缺失时降级策略

---

## 5. 数据来源与映射（事实层）

| 业务字段 | 来源表 | 来源列 | 转换规则 | 备注 |
|---|---|---|---|---|
| `<field>` | `<schema.table>` | `<column>` | `<transform>` | `<note>` |

补充：

1. 来源优先级（主源/备源）：
2. 回退策略：
3. 数据时效语义（实时/盘后/快照）：

---

## 6. 状态语义

1. 页面级状态：
2. 模块级状态（是否 debug-only）：
3. delayed 判定规则：
4. partial 判定规则：

---

## 7. 异常语义

1. 异常对象结构（module/code/severity/message/details）：
2. 用户可见策略：
3. debug 可见策略：

异常码要求：

1. 必须登记到 `wealth/docs/system/exception-code-registry.md`
2. 未登记异常码禁止进入代码与 API 契约

---

## 8. API 契约（需求层）

1. 接口路径：
2. 请求参数：
3. 响应结构：
4. 字段命名规则：
5. 向后兼容策略：

---

## 9. 验收标准

1. 功能验收：
2. 语义验收：
3. 状态验收：
4. 异常验收：

---

## 10. 待拍板项

1. ...
2. ...

---

## 11. 版本记录

| 版本 | 日期 | 变更摘要 | 负责人 |
|---|---|---|---|
| v1 | `<YYYY-MM-DD>` | 初版 | `<owner>` |
